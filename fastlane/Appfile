app_identifier "cc.timebase.ios"
# Apple ID is read from .env (gitignored). Never hardcode personal info
# in tracked files.
apple_id ENV["APPLE_ID"]
team_id "VRNNS4H6XH"
itc_team_id ENV["ASC_TEAM_ID"] # numeric — filled in after first run
